<Window x:Class="CollegeAdmissionAutomation.Lk"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CollegeAdmissionAutomation"
        mc:Ignorable="d"
      Title="Личный кабинет" Height="300" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="Имя:" Grid.Row="0" Margin="5"/>
        <Label Content="{Binding Name}" Grid.Row="0" Margin="5" HorizontalAlignment="Right"/>

        <Label Content="Средний балл:" Grid.Row="1" Margin="5"/>
        <Label Content="{Binding Gpa}" Grid.Row="1" Margin="5" HorizontalAlignment="Right"/>

        <Label Content="Специальность:" Grid.Row="2" Margin="5"/>
        <Label Content="{Binding Spec}" Grid.Row="2" Margin="5" HorizontalAlignment="Right"/>

        <TextBox Grid.Row="3" Margin="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Text="{Binding Status}" IsReadOnly="True"/>
    </Grid>
</Window>